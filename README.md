# semantic-search-with-chromadb
A basic semantic search app using ChromaDB. It reads a text file, splits it into chunks, stores them with embeddings, and lets you search using natural language queries.
